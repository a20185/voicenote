# Local LLM model (development)

Do not import very large GGUF files into the Metro bundle.

Use environment config instead:

- `EXPO_PUBLIC_AI_LOCAL_MODEL_PATH=/absolute/path/to/your-model.gguf`

For this project, we use:

- `/Users/souler/Downloads/Qwen3.5-0.8B-UD-Q4_K_XL.gguf`
